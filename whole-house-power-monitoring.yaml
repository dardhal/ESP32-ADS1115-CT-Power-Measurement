# Hostname : esp32-02
# Friendly Name : power-monitoring
# Summary : Multi channel AC power monitoring for home electrical circuits
# Description : By using SCT-013 CT clamps with 1 V AC output and external ADS1115 ADC, 
#               measure per-circuit AC current for instantaneous and cumulative energy utilization
# Software Used : Latest ESPHome code with two 4-channel ADS1115 ADC 16-bit boards attached to separate I2C buses
#   https://esphome.io/devices/esp32.html
#   https://esphome.io/components/i2c.html
#   https://esphome.io/components/sensor/adc.html
#   https://esphome.io/components/sensor/ct_clamp.html
#
# This is the list of electrical circuits to be monitored (current and voltage are AC)
# 1. Frigorífico    :  5 A / 1 V SCT-013 #1
# 
# 2. Iluminación    : 15 A / 1 V SCT-013 #1
#    Lavadora       :
# 
# 3. Telecom.+Sensor: 5 A / 1 V SCT-013 #2
#    Ventilación    : <<-- pending
# 
# 4. Fuerza Seco    : 10 A / 1 V SCT-013 #1
# 
# 5. Fuerza Húmedos : 30 A / 1 V SCT-013 #1 (incl. Domótica)
#    Horno          :
# 
# 6. Inducción      : 30 A / 1 V SCT-013 #2
#    Lavavajillas   : 
# 
##### 
##### As per something read somewhere else, it may be possible to read incoming / outgoing power using
##### a single SCT-013 and two ADS1115 ADC channels in differential mode, to calculate net power to / from grid
##### Hence these two circuits, which are very low energy consumption, may be repurposed to something else
# 7. Bajo Cubierta :  5 A / 1 V SCT-013 #3
# 
# 8. Bajo Vivienda : 10 A / 1 V SCT-013 #2
#
#####
#
# Last Modified : 2022-02-12
#

esphome:
  name: power-monitoring
  comment: "Home Power Monitoring"
  platform: ESP32
  board: nodemcu-32s

wifi:
  ssid: !secret home_ssid
  password: !secret home_ssid_password
  power_save_mode: none
  fast_connect: on
  use_address: 192.168.1.170
  manual_ip:
    static_ip: 192.168.1.170
    gateway: 192.168.1.1
    subnet: 255.255.255.0
    dns1: 192.168.1.100
    dns2: 192.168.1.10
  ap:
    ssid: "HOME_POWER_MONITORING"
    password: !secret esp_ap_password

time:
  - platform: homeassistant
    id: homeassistant_time
    timezone: "Europe/Madrid"

captive_portal:

logger:
  level: WARN

api:
  password: !secret esphome_api_password

ota:
  safe_mode: True
  password: !secret esphome_ota_password

web_server:
  port: 8888
  auth:
    username: !secret esphome_web_server_username
    password: !secret esphome_web_server_password


# Create single I2C bus where both ADS1115 at addresses 0x48 and 0x49 will be used and
# instantiate the two ADC boards (hubs) at their corresponding addresses
i2c:
  sda: GPIO21
  scl: GPIO22
  scan: true
  frequency: 800kHz
  id: i2c_bus

ads1115:
  - address: 0x48
    continuous_mode: off
    id: ads1115_bus_0x48

  - address: 0x49
    continuous_mode: off
    id: ads1115_bus_0x49


# Board monitoring and power cycling
switch:
  - platform: restart
    name: "Home Power Monitoring Restart"

binary_sensor:
  - platform: status
    name: "Home Power Monitoring Status"

sensor:
  - platform: wifi_signal
    name: "Home Power Monitoring WiFi Level"
    update_interval: 60s

  - platform: homeassistant
    name: "Measured AC Voltage"
    entity_id: sensor.ac_voltage
    unit_of_measurement: "V"
    device_class: voltage
    accuracy_decimals: 1
    id: ha_ac_voltage
    filters:
      - offset: 12.0

#### Sensor definitions for each of the monitored circuits in separate files for readability

# 1. FRIDGE (ADS1115 0x48 , A0_GND)
# 5 A / 1 V SCT-013 #1
  - platform: ads1115
    multiplexer: 'A0_GND'
    gain: 2.048
    name: "Fridge AC Current"
    ads1115_id: ads1115_bus_0x48
    update_interval: 24ms
    id: ac_a_fridge_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply:  5.0                        # Mapping measured voltage from CT clamp to current in the primary circuit (for  5 A / 1 V CT clamp)
      #- lambda: if (x < 0.03) return 0.0; else return x;

  - platform: template
    name: "Fridge AC Power"
    lambda: |-
      if ( id(ac_a_fridge_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 * id(ac_a_fridge_ct_sensor).state) );
      }
      else {
        return ( abs( id(ha_ac_voltage).state * id(ac_a_fridge_ct_sensor).state) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_fridge
  
  - platform: total_daily_energy
    name: "Fridge Energy Consumed"
    power_id: ac_power_fridge
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001
  

# 2. LIGHTING + WASHING MACHINE (ADS1115 0x48, A1_GND)
# 15 A / 1 V SCT-013 #1
  - platform: ads1115
    multiplexer: 'A1_GND'
    gain: 2.048
    name: "Lighting Washing AC Current"
    ads1115_id: ads1115_bus_0x48
    update_interval: 24ms
    id: ac_a_lighting_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply:  15.0                       # Mapping measured voltage from CT clamp to current in the primary circuit (for 15 A / 1 V CT clamp)
      #- offset: -0.08

  - platform: template
    name: "Lighting Washing AC Power"
    lambda: |-
      if ( id(ac_a_lighting_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 * id(ac_a_lighting_ct_sensor).state) );
      }
      else {
        return ( abs(id(ha_ac_voltage).state * id(ac_a_lighting_ct_sensor).state) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_lighting
  
  - platform: total_daily_energy
    name: "Lighting Washing Energy Consumed"
    power_id: ac_power_lighting
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001


# 3. TELECOM/SENSORS + MVHR (ADS1115 0x48, A2_GND)
# 5 A / 1 V SCT-013 #2
# Includes telecom devices, living room TV and deco, DIY temp/RH sensors and (pending) MVHR
  - platform: ads1115
    multiplexer: 'A2_GND'
    gain: 2.048
    name: "Telecom MVHR Washing AC Current"
    ads1115_id: ads1115_bus_0x48
    update_interval: 24ms
    id: ac_a_telecom_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply: 5.0                         # Mapping measured voltage from CT clamp to current in the primary circuit (for  5 A / 1 V CT clamp)

  - platform: template
    name: "Telecom MVHR AC Power"
    lambda: |-
      if ( id(ac_a_telecom_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 * id(ac_a_telecom_ct_sensor).state) );
      }
      else {
        return ( abs(id(ha_ac_voltage).state * id(ac_a_telecom_ct_sensor).state) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_telecom
  
  - platform: total_daily_energy
    name: "Telecom MVHR Energy Consumed"
    power_id: ac_power_telecom
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001


# 4. POWER DRY (ADS1115 0x48, A3_GND)
# 10 A / 1 V SCT-013 #1
# Sockets in dry rooms, including home office and extractor hood
  - platform: ads1115
    multiplexer: 'A3_GND'
    gain: 2.048
    name: "Power Dry AC Current"
    ads1115_id: ads1115_bus_0x48
    update_interval: 24ms
    id: ac_a_powerdry_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply: 10.0                        # Mapping measured voltage from CT clamp to current in the primary circuit (for 10 A / 1 V CT clamp)

  - platform: template
    name: "Power Dry AC Power"
    lambda: |-
      if ( id(ac_a_powerdry_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 * id(ac_a_powerdry_ct_sensor).state ) );
      }
      else {
        return ( abs(id(ha_ac_voltage).state * id(ac_a_powerdry_ct_sensor).state ) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_powerdry
  
  - platform: total_daily_energy
    name: "Power Dry Energy Consumed"
    power_id: ac_power_powerdry
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001


# 5. POWER DAMP + OVEN (ADS1115 0x49, A0_GND)
# 30 A / 1 V SCT-013 #1
# Sockets in damp rooms, including automation server, RPi, switch, etc.
  - platform: ads1115
    multiplexer: 'A0_GND'
    gain: 2.048
    name: "Damp Oven AC Current"
    ads1115_id: ads1115_bus_0x49
    update_interval: 24ms
    id: ac_a_dampoven_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply: 30.0                        # Mapping measured voltage from CT clamp to current in the primary circuit (for 30 A / 1 V CT clamp)

  - platform: template
    name: "Damp Oven AC Power"
    lambda: |-
      if ( id(ac_a_dampoven_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 * id(ac_a_dampoven_ct_sensor).state) );
      }
      else {
        return ( abs(id(ha_ac_voltage).state * id(ac_a_dampoven_ct_sensor).state) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_dampoven
  
  - platform: total_daily_energy
    name: "Damp Oven Energy Consumed"
    power_id: ac_power_dampoven
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001


# 6. INDUCTION HOB + DISHWASHER (ADS1115 0x49, A1_GND)
# 30 A / 1 V SCT-013 #2
  - platform: ads1115
    multiplexer: 'A1_GND'
    gain: 2.048
    name: "Hob Dishwasher AC Current"
    ads1115_id: ads1115_bus_0x49
    update_interval: 24ms
    id: ac_a_hobdishwasher_ct_sensor
    state_class: measurement
    device_class: current
    unit_of_measurement: "A"
    icon: "mdi:flash"
    accuracy_decimals: 8
    filters:
      - lambda: return x * x;                 ####
      - sliding_window_moving_average:        #
          window_size: 1250                   # This blocks calculates the RMS voltage sampled by the ADS1115 ADC (averages over 30 s and reports every 15 s)
          send_every:  625                    #
      - lambda: return sqrt(x);               ####
      - multiply: 30.0                        # Mapping measured voltage from CT clamp to current in the primary circuit (for 30 A / 1 V CT clamp)

  - platform: template
    name: "Hob Dishwasher AC Power"
    lambda: |-
      if ( id(ac_a_hobdishwasher_ct_sensor).state < 0 ) {
        return 0;
      }
      if ( id(ha_ac_voltage).state < 180 ) {
        return ( abs(230.0 *  id(ac_a_hobdishwasher_ct_sensor).state ) );
      }
      else {
        return ( abs(id(ha_ac_voltage).state * id(ac_a_hobdishwasher_ct_sensor).state ) );
      }
    accuracy_decimals: 1
    update_interval: 30s
    state_class: measurement
    device_class: power
    unit_of_measurement: "W"
    id: ac_power_hobdishwasher
  
  - platform: total_daily_energy
    name: "Hob Dishwasher Energy Consumed"
    power_id: ac_power_hobdishwasher
    unit_of_measurement: kWh
    filters:
      - multiply: 0.001


##### 7. ATTIC (ADS1115 0x49, A2_GND)
# 5 A / 1 V SCT-013 #3
##### 8. BASEMENT (ADS1115 0x49, A3_GND)
# 10 A / 1 V SCT-013 #2

#### OR
#### Differential input between A2_GND and A3_GND for + and - leads coming from SCT-013 CT, to measure net power in / out the grid
####

